[toolchain]
package_manager = "yarn"

[features]
resolution = true
skip-lint = false

[programs.localnet]
program = "<Program ID here>"

[programs.devnet]
program = "<Program ID here>"

[provider]
cluster = "devnet"
wallet = "/tmp/test-wallet.json"

[scripts]
test = "cargo test"

[hooks]
